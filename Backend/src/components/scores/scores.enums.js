export const SCORES_STATUSES = Object.freeze({
  ACTIVE: "active",
  INACTIVE: "inactive",
  ARCHIVED: "archived",
});

export const SCORE_SOURCES = Object.freeze({
  USER: "user",
  ADMIN: "admin",
});

export const SCORES_COMPETITION_RULES = Object.freeze({
  QUALIFYING_WINDOW_SIZE: 5,
});

export const SCORES_ELIGIBILITY_REASONS = Object.freeze({
  INSUFFICIENT_QUALIFYING_SCORES: "insufficient_qualifying_scores",
  DUPLICATE_CONTEST_NUMBERS: "duplicate_contest_numbers",
});
