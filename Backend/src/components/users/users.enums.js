export const USER_SORT_FIELDS = Object.freeze({
  CREATED_AT: "createdAt",
  DISPLAY_NAME: "displayName",
  EMAIL: "email",
});

export const PROFILE_SORT_FIELDS = Object.freeze({
  CREATED_AT: "createdAt",
  UPDATED_AT: "updatedAt",
  LAST_NAME: "lastName",
});
